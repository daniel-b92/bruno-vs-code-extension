meta {
  name: other_request
  type: http
  seq: 1
}

get {
  url: https://example.com/{{blo}}
  body: none
  auth: none
}

vars:pre-request {
  pre-request1: asdsdsd
}

vars:post-response {
  post-response1: asdsdsdtt
}

script:pre-request {
  bru.setEnvVar("blo", "asas");
  const var = bru.getVar("pre-request1");
}

script:post-response {
  bru.setVar("exe", "asasas");
  const var = bru.getVar("post-response1");
}

tests {
  test("should be able to login", function () {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.contain("example");
  });
}
